# Dolgator

Минималистичный счётчик калорий (граммы) и повторений в упражнениях. Личное приложение без сервера — все данные хранятся на телефоне.

## Стек

- **React Native** + **Expo 52**
- **AsyncStorage** — локальное сохранение
- TypeScript

## Быстрый старт

### 1. Установите Node.js

Скачайте LTS с [nodejs.org](https://nodejs.org/) (версия 20+), перезапустите терминал.

### 2. Установите зависимости

```bash
cd dolgator
npm install
```

### 3. Запуск на ноутбуке (браузер)

```bash
npm run web
```

Откроется в браузере — можно проверить логику и интерфейс.

### 4. Запуск на Android

```bash
npm start
```

- Установите **Expo Go** на телефон
- Отсканируйте QR-код из терминала

Или подключите эмулятор Android Studio и нажмите `a` в терминале Expo.

### 5. Сборка APK для Android (тест на телефоне)

#### Вариант A — облачная сборка EAS (рекомендуется)

Не нужны Android Studio и JDK. Сборка на серверах Expo, в конце — **ссылка на скачивание APK**.

```bash
# один раз: войти в аккаунт Expo (бесплатный)
npx eas login

# один раз: привязать проект (создаст projectId в app.json)
npx eas init

# собрать оптимизированный APK (~5–15 мин)
npm run build:apk
```

После сборки в терминале появится ссылка вида `https://expo.dev/accounts/.../builds/...` — откройте на телефоне и скачайте APK.

**Установка на телефон:**
1. Скачайте APK по ссылке
2. Разрешите установку из неизвестных источников (если спросит)
3. Установите и запустите **Dolgator**

При следующих версиях перед сборкой обновите в `app.json`:
- `version` — как в `src/version.ts` (например `0.1.6`)
- `android.versionCode` — целое число, каждый релиз +1 (6, 7, 8…)

#### Вариант B — локальная сборка (Windows)

Нужны: **JDK 17**, **Android Studio** (SDK + build-tools).

```bash
npm install
npm run build:apk:local
```

APK будет здесь:
`android/app/build/outputs/apk/release/app-release.apk`

> Локальный release может потребовать настройки подписи (keystore). Для быстрого теста проще вариант A.

#### Оптимизация

В release-сборке включены:
- **Minify** (Proguard/R8) — уменьшение размера кода
- **Shrink resources** — удаление неиспользуемых ресурсов

Настройки в `app.json` → plugin `expo-build-properties`.

## Как пользоваться

### Упражнения (верх — фиолетовый)

1. Регулируйте повторения кнопками **−** и **+**
2. По очереди: **ноги → грудь → спина** (3 упражнения = 1 подход)
3. **Нажмите на таблицу** — записать текущее значение (кнопка ОК)
4. После 3-го упражнения подход целиком попадает в строку таблицы
5. Строка **суммы** — всего повторений за день

### Еда (низ — голубой)

1. Взвешивайте порцию, выставляйте граммы **−** / **+** (шаг 10 г)
2. **Нажмите на таблицу** — записать приём пищи
3. Максимум **5 приёмов** в день
4. Строка **сумма** — всего грамм за день

### Неделя

Колонки — дни текущей недели (пн–вс). Сегодняшний день подсвечен.

## Структура проекта

```
dolgator/
├── App.tsx                 # Главный экран
├── src/
│   ├── components/         # UI: таблица, счётчик
│   ├── hooks/              # useTrackerData
│   ├── storage/            # AsyncStorage
│   ├── theme/              # Неоновые цвета
│   └── types/              # TypeScript типы
└── main-screen.png         # Эскиз интерфейса
```

## Дальше (TODO)

- [ ] Экспорт данных в JSON одной кнопкой
- [ ] Сравнение недель визуально
- [ ] Кастомные иконки и splash screen
